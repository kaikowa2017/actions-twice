# GitHub App credentials (required for webhook)
APP_ID=
PRIVATE_KEY=
WEBHOOK_SECRET=

# Public slug for the install CTA on the landing page
# e.g. if your app URL is https://github.com/apps/actions-twice, slug is actions-twice
NEXT_PUBLIC_GITHUB_APP_SLUG=
